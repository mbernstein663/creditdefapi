# Demo Assets

This folder holds committed demo inputs and outputs for API and batch scoring.

- `sample_batch_input.csv`: small synthetic input for batch scoring.
- `sample_batch_output.csv`: scored output generated from the current saved accepted-loan bundle.

Screenshot/GIF placeholders:

- `docs/demo/api-screenshot.png`
- `docs/demo/api-demo.gif`

No media is committed here yet. Capture a browser screenshot or short screen recording after running the API locally, then save it to one of the paths above.

For a fresh-clone runbook, see [RUNBOOK.md](../../RUNBOOK.md).
